Usage: maven_batch_deploy.sh <task_id> <env> [-h] [-y] [-b <branch>]
    -h  显示帮助文档
    -y  直接切换，无需确认
    -b  使用指定分支编译发布

    task_id 可以通过 `task_manage.sh show` 查看
    env     要编译发布的环境

example: maven_batch_deploy.sh 1 dev -y
    批量合并task_id为1的所有项目到sit分支，无需确认
example: maven_batch_deploy.sh 1 sit
    批量合并task_id为1的所有项目的开发分支到branch1，需要确认
example: maven_batch_deploy.sh 1 -t branch1 -f branch2
    批量合并task_id为1的所有项目的 branch2 到 branch1，需要确认

